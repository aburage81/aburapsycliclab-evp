name: Update EVP Data (韓国・台湾シャーマニズム強化)

on:
  schedule:
    - cron: '*/30 * * * *'   # 30分ごと
  workflow_dispatch:         # 手動実行可能

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # コミット履歴を確保

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser deep-translator

      - name: Run crawler script (韓国・台湾ホラー強化版)
        run: python .github/scripts/update_evp.py

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add index.html
          git commit -m "🕯️ Auto-update: $(date -u +'%Y-%m-%d %H:%M') [韓国・台湾シャーマニズム]" || exit 0
          git push
